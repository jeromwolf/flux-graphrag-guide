# PRD: 제조 도메인 GraphRAG 유스케이스 프로젝트
# Manufacturing Domain GraphRAG Use Case Project

> **프로젝트명**: manufacturing-graphrag
> **작성일**: 2026-02-07
> **작성자**: Jeromwolf (Root Bricks Co., Ltd.)
> **실행 환경**: Claude Code
> **관련 문서**: 
>   - `00_master_roadmap.md` (마스터 로드맵 v2.0)
>   - `graphrag_curriculum_guide_v3_0.md` (커리큘럼 v3.0)
>   - `domain_project_briefing.md` (브리핑 문서)

---

## 1. 프로젝트 개요

### 1-1. 목적

자동차 부품(브레이크 패드) 제조 공정을 대상으로 GraphRAG 유스케이스를 **4단계(0~3단계)** 로 구축한다. 이를 통해:

1. GraphRAG 교육 콘텐츠의 실습 자료로 활용
2. 한국 GraphRAG 가이드 사이트의 첫 번째 도메인 레퍼런스 확보
3. 이후 금융/법률/IT 도메인 확장의 템플릿 역할

### 1-2. 배경

- 온톨로지 + GraphRAG 실무 과정 (11시간, v3.0) 커리큘럼 확정 완료
- 벡터 RAG 대비 GraphRAG의 Multi-hop 추론 가치를 도메인 유스케이스로 증명 필요
- 한국 시장에 GraphRAG 체계적 가이드 부재 → 선점 기회

### 1-3. 핵심 원칙

```
- "GraphRAG부터 시작하지 마라. 문제 정의부터 하라"
- "처음부터 서비스급으로 하면 다들 모른다" → 미니 데모부터 점진 확장
- "깊이가 곧 가치" — 하나를 완벽하게, 나머지는 복제
- "어려운 것을 쉽게 설명한다"
```

---

## 2. 도메인 정의

### 2-1. 대상 도메인

| 항목 | 내용 |
|------|------|
| 도메인 | 자동차 부품 제조 |
| 하위 시나리오 | 브레이크 패드 (디스크 브레이크 패드) |
| 공정 | 혼합 → 예비성형 → 열압착 → 열처리 → 연마 → 도장 → 조립 (7단계) |
| GraphRAG 필요성 | ⭐⭐⭐⭐ (높음) — 결함 원인의 Multi-hop 인과관계 추론 |

### 2-2. 핵심 질의 (GraphRAG가 답해야 할 것)

| ID | 질의 | 추론 유형 | Hop수 |
|----|------|----------|-------|
| Q1 | "접착 박리 결함의 근본 원인 공정과 설비는?" | Multi-hop | 3 |
| Q2 | "HP-01 설비 고장 시 영향받는 제품은?" | 역방향 전파 | 2 |
| Q3 | "불량률 높은 공정의 담당 작업조는?" | 관계 집계 | 2 |
| Q4 | "마찰재 공유 제품에 동일 결함 위험?" | 횡적 연결 | 3 |

### 2-3. 온톨로지 구조

```
엔티티 7종:
  Product(제품), Component(부품/원자재), Process(공정),
  Equipment(설비), Defect(결함), Inspection(검사), Worker(작업자)

관계 11종:
  COMPOSED_OF, PRODUCED_BY, NEXT, USES_EQUIPMENT, USES_MATERIAL,
  DETECTED_AT, CAUSED_BY_PROCESS, CAUSED_BY_EQUIPMENT, 
  CAUSED_BY_MATERIAL, INSPECTS, OPERATES, ASSIGNED_TO
```

---

## 3. 4단계 구조 + 커리큘럼 매핑

### 3-1. 전체 구조

```
0단계 (미니 데모)  ↔ Part 1     — 노드 7개, 전체 파이프라인 한 바퀴
1단계 (교육용)    ↔ Part 2~3   — 노드 35개, 수작업 vs LLM 비교
2단계 (프로토)    ↔ Part 4~6   — 노드 500~1K, Agent + 하이브리드 검색
3단계 (서비스급)  ↔ Part 7     — 노드 5K+, 평가 + 모니터링 + 배포
```

### 3-2. 각 단계별 요구사항

#### 0단계: 미니 데모 (✅ 설계 완료, Claude Code에서 구현 필요)

```
목표: 5분 만에 "GraphRAG가 이런 거구나" 체험
입력: 이미 작성된 스키마/데이터/Cypher 파일
구현:
  ├── Neo4j Docker 환경 셋업 (docker-compose.yml)
  ├── 자동 적재 스크립트 (Python — neo4j 드라이버)
  ├── 기본 Text2Cypher 동작 확인
  ├── 결과 시각화 (터미널 + Neo4j Browser)
  └── 원커맨드 실행: make stage0 또는 ./run_stage0.sh
Milestone: Neo4j에서 결함→공정→설비 3-hop 시각화 확인
```

**기존 산출물 (이 세션에서 작성 완료):**
- `stage-0-mini/00_mini_schema.yaml`
- `stage-0-mini/01_mini_data.json`
- `stage-0-mini/02_neo4j_load.cypher`
- `stage-0-mini/03_first_query.cypher`
- `stage-0-mini/04_basic_text2cypher.py`

**Claude Code에서 추가 구현:**
- [ ] `docker-compose.yml` — Neo4j 컨테이너 환경
- [ ] `stage-0-mini/run.py` — 원커맨드 자동 실행 (적재 + 질의 + 결과 출력)
- [ ] `stage-0-mini/requirements.txt`
- [ ] `Makefile` — make stage0 진입점

#### 1단계: 교육용 데모

```
목표: 수작업 KG → LLM 자동 KG → 품질 비교
입력: 이미 작성된 스키마/데이터 + 제조 관련 텍스트 문서
구현:
  ├── stage-1-education/
  │   ├── 01_ontology_schema.yaml         (✅ 기존 v1.1)
  │   ├── 02_sample_data.json             (✅ 기존 v1.1)
  │   ├── 03_meta_dictionary.json         (관계 패턴 사전, Prefix 9가지)
  │   ├── 04_neo4j_load.py                (Python 기반 자동 적재)
  │   ├── 05_manual_extraction_guide.md   (수작업 추출 가이드)
  │   ├── 06_entity_extraction_prompt.md  (LLM 엔티티 추출 프롬프트)
  │   ├── 07_llm_auto_extract.py          (LLM 자동 추출 실행)
  │   ├── 08_manual_vs_llm_compare.py     (수작업 vs LLM 비교 리포트)
  │   ├── 09_text2cypher_basic.py         (기본 Text2Cypher, 질의 4개)
  │   └── 10_visualization_guide.md       (Neo4j 시각화 가이드)
  └── sample_docs/                         (추출 대상 텍스트 문서)
      └── brake_pad_manufacturing.md       (브레이크 패드 공정 설명서)
Milestone: 수작업 KG + LLM KG + 품질 비교 리포트
```

#### 2단계: 프로토타입

```
목표: 자연어 질문 → 그래프 검색 → 답변 생성 전체 파이프라인
입력: 1단계 KG + 확장 데이터 (노드 500~1,000)
구현:
  ├── stage-2-prototype/
  │   ├── 01_extended_schema.yaml
  │   ├── 02_data_generator.py            (대규모 샘플 데이터 생성)
  │   ├── 03_entity_resolution.py         (ER: Fuzzy + 임베딩 + LLM)
  │   ├── 04_vlm_table_extraction.py      (VLM 표→KG)
  │   ├── 05_text2cypher_agent.py         (LangGraph Agent)
  │   ├── 06_hybrid_search.py             (벡터 + 그래프 + RRF)
  │   ├── 07_prompt_routing.py            (Query→Task→Prompt Pool)
  │   ├── 08_streamlit_demo.py            (Streamlit UI)
  │   └── docker-compose.yml
  └── sample_docs/
      ├── inspection_reports/              (검사 보고서 PDF/이미지)
      └── defect_tables/                   (결함 통계 표)
Milestone: Streamlit에서 자연어 질문 → 답변 + 그래프 시각화
```

#### 3단계: 서비스급

```
목표: 평가 지표 기반 개선 사이클 + 배포 가능한 시스템
입력: 2단계 시스템 + 대규모 데이터 (노드 5,000+)
구현:
  ├── stage-3-production/
  │   ├── 01_production_schema.yaml
  │   ├── 02_data_pipeline.py             (자동 데이터 파이프라인)
  │   ├── 03_ragas_evaluation.py          (RAGAS: Multi-hop + CK)
  │   ├── 04_evaluation_dataset.json      (평가셋: Easy/Medium/Hard)
  │   ├── 05_neo4j_optimization.md        (성능 최적화 7가지)
  │   ├── 06_gdbms_checklist.md           (GDBMS 선정 가이드)
  │   ├── 07_monitoring_setup.py          (LangSmith/LangFuse)
  │   ├── 08_cost_optimization.md         (비용 최적화)
  │   ├── 09_troubleshooting.md           (트러블슈팅)
  │   ├── 10_streamlit_dashboard.py       (품질 대시보드)
  │   ├── docker-compose.yml              (전체 배포)
  │   └── Makefile
Milestone: 배포 가능한 GraphRAG + 품질 평가 대시보드
```

---

## 4. 기술 스택

| 영역 | 도구 | 버전/비고 |
|------|------|----------|
| 언어 | Python | 3.11+ |
| 그래프 DB | Neo4j Community | 5.x, Docker |
| Neo4j 드라이버 | neo4j (Python) | 최신 |
| LLM | OpenAI GPT-4o | 기본, Claude 대체 가능 |
| LLM 프레임워크 | LangChain / LangGraph | 최신 |
| VLM | Upstage Document Parse / Qwen-VL | 2단계 |
| 임베딩 | OpenAI text-embedding-3-small | 2단계 |
| 벡터 DB | Neo4j Vector Index | 2단계 (별도 DB 불필요) |
| ER | Fuzzy (thefuzz) + 임베딩 유사도 | 2단계 |
| 평가 | RAGAS | 3단계 |
| 모니터링 | LangSmith / LangFuse | 3단계 |
| UI | Streamlit | 2~3단계 |
| 컨테이너 | Docker Compose | 전체 |
| 빌드 | Makefile | 전체 |

---

## 5. 프로젝트 구조

```
manufacturing-graphrag/
│
├── README.md                          # 프로젝트 소개 + 빠른 시작
├── Makefile                           # make stage0 / make stage1 / ...
├── docker-compose.yml                 # Neo4j + 앱 환경
├── requirements.txt                   # 공통 의존성
├── .env.example                       # 환경변수 템플릿
│
├── docs/
│   ├── 00_master_roadmap.md           # 마스터 로드맵 v2.0
│   ├── curriculum_mapping.md          # 커리큘럼 v3.0 매핑
│   └── architecture.md                # 전체 아키텍처 다이어그램
│
├── stage-0-mini/                      # ⭐ 0단계 (노드 7개, 5분 시작)
│   ├── README.md
│   ├── 00_mini_schema.yaml
│   ├── 01_mini_data.json
│   ├── 02_neo4j_load.cypher
│   ├── 03_first_query.cypher
│   ├── 04_basic_text2cypher.py
│   ├── run.py                         # 원커맨드 실행
│   └── requirements.txt
│
├── stage-1-education/                 # 1단계 (노드 35개, 수작업 vs LLM)
│   ├── README.md
│   ├── ...
│   └── requirements.txt
│
├── stage-2-prototype/                 # 2단계 (노드 1K, Agent + 검색)
│   ├── README.md
│   ├── ...
│   └── docker-compose.yml
│
├── stage-3-production/                # 3단계 (노드 5K+, 평가 + 배포)
│   ├── README.md
│   ├── ...
│   └── docker-compose.yml
│
├── shared/                            # 공유 유틸리티
│   ├── neo4j_utils.py                 # Neo4j 연결/적재 공통 함수
│   ├── llm_utils.py                   # LLM 호출 공통 함수
│   └── config.py                      # 설정 관리
│
└── sample_docs/                       # 샘플 문서 (추출 대상)
    ├── brake_pad_manufacturing.md
    ├── inspection_reports/
    └── defect_tables/
```

---

## 6. 구현 우선순위 (Claude Code 작업 순서)

### Phase 1: 인프라 + 0단계 동작 (최우선)

```
우선순위 1 ─── 프로젝트 초기화
├── [ ] GitHub 레포 초기화 (manufacturing-graphrag)
├── [ ] README.md (프로젝트 소개 + 빠른 시작 가이드)
├── [ ] docker-compose.yml (Neo4j 환경)
├── [ ] .env.example
├── [ ] Makefile (make stage0)
├── [ ] shared/neo4j_utils.py (연결/적재 공통 함수)
├── [ ] shared/llm_utils.py (LLM 호출 공통 함수)
├── [ ] shared/config.py
└── [ ] requirements.txt

우선순위 2 ─── 0단계 완성 (원커맨드 실행)
├── [ ] 기존 파일 이관 (스키마/데이터/Cypher/Text2Cypher)
├── [ ] stage-0-mini/run.py (자동 실행: 적재→질의→결과 출력)
├── [ ] stage-0-mini/requirements.txt
└── [ ] 동작 테스트: make stage0 → 결과 확인
```

### Phase 2: 1단계 교육용

```
우선순위 3 ─── 1단계 핵심 파일
├── [ ] 기존 스키마/데이터 이관 → stage-1-education/
├── [ ] 03_meta_dictionary.json (관계 패턴 사전)
├── [ ] 04_neo4j_load.py (Python 자동 적재)
├── [ ] sample_docs/brake_pad_manufacturing.md (추출 대상 문서)
├── [ ] 06_entity_extraction_prompt.md
├── [ ] 07_llm_auto_extract.py
├── [ ] 08_manual_vs_llm_compare.py
├── [ ] 09_text2cypher_basic.py (질의 4개)
└── [ ] make stage1 동작 확인
```

### Phase 3: 2단계 프로토타입

```
우선순위 4 ─── 2단계 핵심 파일
├── [ ] 데이터 생성기 (노드 500~1K)
├── [ ] Entity Resolution
├── [ ] Text2Cypher Agent (LangGraph)
├── [ ] 하이브리드 검색 (벡터 + 그래프)
├── [ ] Streamlit 데모
└── [ ] make stage2 동작 확인
```

### Phase 4: 3단계 서비스급

```
우선순위 5 ─── 3단계 핵심 파일
├── [ ] RAGAS 평가 파이프라인
├── [ ] 평가 데이터셋 설계
├── [ ] Neo4j 성능 최적화 적용
├── [ ] 모니터링 설정
├── [ ] Docker Compose 전체 배포
└── [ ] make stage3 동작 확인
```

---

## 7. 환경 설정

### 7-1. Docker Compose (Neo4j)

```yaml
# docker-compose.yml
services:
  neo4j:
    image: neo4j:5-community
    ports:
      - "7474:7474"   # Browser
      - "7687:7687"   # Bolt
    environment:
      - NEO4J_AUTH=neo4j/graphrag2026
      - NEO4J_PLUGINS=["apoc"]
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs

volumes:
  neo4j_data:
  neo4j_logs:
```

### 7-2. 환경변수

```bash
# .env
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=graphrag2026
OPENAI_API_KEY=sk-...
```

### 7-3. Python 의존성

```
# requirements.txt
neo4j>=5.0
openai>=1.0
langchain>=0.2
langgraph>=0.1
streamlit>=1.30
python-dotenv>=1.0
pyyaml>=6.0
thefuzz>=0.20        # 2단계
ragas>=0.1           # 3단계
langsmith>=0.1       # 3단계
```

---

## 8. 성공 기준

### 단계별 Milestone

| 단계 | Milestone | 검증 방법 |
|------|-----------|----------|
| 0단계 | `make stage0` → 결함 원인 3-hop 추적 결과 출력 | 터미널 출력 확인 |
| 1단계 | 수작업 KG vs LLM KG 비교 리포트 생성 | 비교표 파일 생성 확인 |
| 2단계 | Streamlit에서 자연어 질문 → 답변 + 그래프 | 브라우저 데모 동작 |
| 3단계 | RAGAS 평가 점수 + 품질 대시보드 | 평가 리포트 생성 확인 |

### 전체 프로젝트 성공 기준

```
1. 4단계 전체가 make 명령 하나로 실행 가능
2. 각 단계에서 이전 단계 산출물을 자연스럽게 확장
3. 커리큘럼 v3.0의 Part 1~7과 1:1 매핑
4. 이후 금융/법률/IT 도메인으로 복제 가능한 템플릿 구조
5. 한글/영어 병기로 국내외 활용 가능
```

---

## 9. 참고 자료

| 자료 | 위치 | 용도 |
|------|------|------|
| 마스터 로드맵 v2.0 | `docs/00_master_roadmap.md` | 전체 전략 + 타임라인 |
| 커리큘럼 v3.0 | `graphrag_curriculum_guide_v3_0.md` | Part별 매핑 기준 |
| 브리핑 문서 | `domain_project_briefing.md` | 도메인 후보 + 기술 스택 |
| 정이태님 강의 | YouTube (테디노트) | GraphRAG 핵심 인사이트 |
| LangGraph Text2Cypher | LangChain 공식 문서 | Agent 구현 참고 |

---

## 10. 주의사항

```
1. 0단계가 동작하지 않으면 나머지는 의미 없음 → 0단계 품질 최우선
2. Neo4j는 Community Edition 사용 (라이선스 무료)
3. LLM API 키 없이도 0단계는 동작해야 함 (Cypher 직접 실행)
4. 각 단계는 독립 실행 가능해야 함 (0단계만, 또는 1단계만)
5. sample_docs/의 문서는 실제 제조 도메인 전문성 반영 필요
6. KB국민카드 프로젝트 관련 내용은 포함하지 않음
```

---

> 💡 **Claude Code에게**: 
> 이 PRD를 기반으로 Phase 1(인프라 + 0단계)부터 순차적으로 구현하세요.
> 0단계 `make stage0`가 동작하는 것이 최우선 목표입니다.
> 기존 산출물(stage-0-mini/ 디렉토리)을 그대로 활용하되,
> 실행 자동화(run.py, Makefile, docker-compose)를 추가하세요.
